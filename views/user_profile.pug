- var name = obj.fname + ' ' + obj.lname
- var url = 'https://api.adorable.io/avatars/150/' + obj.userid

doctype html
head
  title= "User Profile"
body
  h1 #{name}
  img(src=url)
  p
    Profile viewed #{obj.views} times!
  a(href='/user') user list
